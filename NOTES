numbers: (decimal)
  #0
  #100

numbers: (hex)
  $0
  $deadbeef

numbers: (binary)
  ~0
  ~1001

addresses: (hex)
  @ff
  @0
  dereference with *

function labels
  foo:

variable names
  foo => #0
  foo => nop // what's on the stack after the instruction

video:
  request a terminal
    80x25 or more
  get a pointer back with the terminal memory (and size)
